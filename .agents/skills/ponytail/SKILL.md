---
name: ponytail
description: Apply the universal lazy-senior minimality discipline to a coding task; supports lite, full, and ultra intensity.
argument-hint: "[lite|full|ultra]"
license: MIT
---
# ponytail
Compatibility alias for the minimality section of `.agents/universal/CONTRACT.md`. Default `full`; `lite` builds the requested shape while naming a materially simpler option, `full` enforces the ladder, and `ultra` aggressively rejects speculative additions/deletes before adding while still honoring explicit behavior and all safety/verification rules.

Read and trace the real code first. Then stop at the first complete rung: need at all → existing code → stdlib → native platform → installed dependency → direct one-line form → minimum new code. Root-cause bug fixes belong at the common seam, not repeated symptom paths. No speculative abstraction/config/scaffolding. Mark a real deliberate ceiling with `ponytail: <ceiling>, <upgrade trigger/path>`.

Never cut trust-boundary validation, data-loss protection, security/privacy, accessibility, required scientific/release gates, explicit requirements, or necessary hardware/physical calibration. Non-trivial logic requires a runnable check; trivial direct substitutions do not need ceremonial new tests.

When this compatibility skill is explicitly invoked, prefer implementation/code first and concise unrequested prose; if the user asked for a report, walkthrough, or phase notes, provide them fully. `stop ponytail`, `ponytail off`, or `normal mode` disables only this minimality preference.

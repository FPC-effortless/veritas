---
name: ponytail-gain
description: Show Ponytail's published benchmark impact with methodology labels and a strict no-per-repo-extrapolation boundary.
---
# ponytail-gain
One-shot informational display; changes no mode/state.

Always distinguish the two upstream benchmark regimes preserved at the pinned fusion source:

**Later agentic benchmark (README foreground):** 12 feature tasks, n=4, Haiku 4.5; approximately `LOC -54%`, `tokens -22%`, `cost -20%`, `time -27%`, `safety 100%` on that stated benchmark.

**Legacy isolated-generation scoreboard (upstream gain skill):** 5 everyday tasks across Haiku/Sonnet/Opus; reported `lines -80–94%`, `cost -47–77%`, `speed 3–6x`.

Do not blend these methods. These are upstream benchmark results, not the current repository. Never print “you saved X here” without a controlled live baseline. For repo-specific inspectable evidence, point to `ponytail-debt` and `ponytail-audit`, not invented savings.
